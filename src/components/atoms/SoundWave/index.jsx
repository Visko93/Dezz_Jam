import * as React from 'react'
import { WaveBase } from './style'

const SoundWave = () => {
  return (
    <WaveBase>
      <canvas id="canvas"></canvas>
    </WaveBase>
  )
}

export default SoundWave
